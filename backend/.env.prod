# .env file (Example for production - DO NOT commit sensitive URLs to Git)
# DATABASE_URL=postgres://USER:PASSWORD@HOST:PORT/DBNAME
# DATABASE_SSL_REQUIRE=True # If your prod DB requires SSL
SECRET_KEY='your_strong_production_secret_key'
DEBUG=False
# Allowed hosts will also need setting in production
# ALLOWED_HOSTS=yourdomain.com